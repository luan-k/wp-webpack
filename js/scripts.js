import "../css/style.css";

import "./modules/animations";
import "./modules/anime";
import "./modules/carousel";
import "./modules/carousel";
import "./modules/whitebg.png";

// Allow new JS and CSS to load in browser without a traditional page refresh
if (module.hot) {
  module.hot.accept();
}
